# Projeto Frontend

Como rodar:
1. npm install
2. npm run dev

Sobre o backend:
Os dados de teste ficam em src/servicos/dadosFake.js.
No projeto eu coloquei a parte do backend com um comentário
// >>> BACKEND: trocar por <<<
Isso vai mostrar exatamente onde colocar as chamadas da API.